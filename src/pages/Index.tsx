/** Home — civilization picker. Full civ grid arrives in M7; M1 ships the shell. */
export default function Index() {
  return (
    <section className="page-enter mx-auto max-w-3xl text-center">
      <h1 className="font-display text-4xl font-bold tracking-wide">Age of Plan</h1>
      <p className="mt-4 text-muted-foreground">
        Create, edit, import, export, and follow Age of Empires IV build orders —
        with a chrome-less overlay for a second monitor or your phone.
      </p>
      <p className="mt-8 text-sm text-muted-foreground">
        Pick a civilization to get started. (Coming soon.)
      </p>
    </section>
  );
}
