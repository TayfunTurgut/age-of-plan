/** Create flow — pick a civ + name, then create the build. Built out in M9. */
export default function NewBuildOrder() {
  return (
    <section className="page-enter">
      <h1 className="font-display text-3xl font-bold">New build order</h1>
      <p className="mt-2 text-muted-foreground">
        Choose a civilization and name your build to get started.
      </p>
    </section>
  );
}
